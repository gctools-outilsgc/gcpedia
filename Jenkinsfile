pipeline {
    agent any

    stages {
        stage('Build') { 
            steps { 
                echo 'build docker image'
            }
        }
        stage('Test'){
            steps {
                echo 'run automated tests'
            }
        }
    }
}
